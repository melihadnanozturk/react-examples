spring:
  application:
    name: redux-crud-api
  datasource:
    url: jdbc:h2:mem:reduxdb
    username: redux
    password: maoco
    driverClassName: org.h2.Driver
  sql:
    init:
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true


